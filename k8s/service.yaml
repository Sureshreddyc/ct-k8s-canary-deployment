apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: canary
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
